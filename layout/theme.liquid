{% comment %}

  © 2018 Atlas Group, Inc. All rights reserved.

  No portion of this software code may be reproduced, distributed or modified
  without prior express written authorization of Atlas Group, Inc.

  Unauthorized reproduction or copying of this software code, via any medium,
  is strictly prohibited and may subject you to severe legal liability.

{% endcomment %}

<!doctype html>

<html lang="{{ shop.locale }}">

  <head>

    <title>

      {{ page_title }}

    </title>


    <!-- Global site tag (gtag.js) - Google Ads: 778854796 --> <script async src="https://www.googletagmanager.com/gtag/js?id=AW-778854796&ckcachebust=586898796"></script> <script> window.dataLayer = window.dataLayer || []; function gtag(){dataLayer.push(arguments);} gtag('js', new Date()); gtag('config', 'AW-778854796'); </script>


    <meta charset="utf-8">

    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <meta name="description" content="{{ page_description | escape }}">

    <!-- Facebook Share -->

    <meta property="og:type" content="website">

    <meta property="og:title" content="{{ page_title }}">

    <meta property="og:description" content="{{ page_description }}">

    <meta property="og:image" content="https://cdn.shopify.com/s/files/1/0063/2333/9362/files/KiiTO_coverimage.jpg?3390432515457538695">

    <meta property="og:image:secure_url" content="https://cdn.shopify.com/s/files/1/0063/2333/9362/files/KiiTO_coverimage.jpg?3390432515457538695">

    <!-- Twitter Share -->

    <meta name="twitter:card" content="summary_large_image">

    <meta name="twitter:title" content="{{ page_title }}">

    <meta name="twitter:description" content="{{ page_description }}">

    <meta name="twitter:image" content="https://cdn.shopify.com/s/files/1/0063/2333/9362/files/KiiTO_coverimage.jpg?3390432515457538695">

    <meta name="viewport" content="width=device-width , initial-scale=1 , maximum-scale=1 , shrink-to-fit=no , user-scalable=no">

     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick-theme.min.css?ckcachebust=586898796">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/fancyapps/fancybox@3.5.6/dist/jquery.fancybox.min.css?ckcachebust=586898796" />

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.css?ckcachebust=586898796">

    <script async type="text/javascript" data-api-key="" id="stamped-script-widget" src="https://cdn-stamped-io.azureedge.net/files/widget.min.js?ckcachebust=586898796"></script>

    {% if settings.favicon != blank %}

      <link rel="shortcut icon" type="image/png" href="{{ settings.favicon | img_url : '32x32' }}">

    {% endif %}

    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css?ckcachebust=586898796" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">

    <link rel="canonical" href="{{ canonical_url }}">

    {% include 'style-tags' , layout : 'theme' %}

    {{- 'styles.css' | asset_url | stylesheet_tag -}}

      <!--[if (gt IE 9)|!(IE)]><!--><script src="{{ 'g-addtocart.js' | asset_url }}" defer="defer"></script><!--<![endif]-->
  <!--[if lte IE 9]><script src="{{ 'g-addtocart.js' | asset_url }}"></script><![endif]-->

    {{ content_for_header }}

    <style>

      @font-face {

        src : url( '{{ "chalet-comprime-cologne-sixty.woff2" | asset_url }}' ) format( 'woff2' ),

              url( '{{ "chalet-comprime-cologne-sixty.woff" | asset_url }}' ) format( 'woff2' );

        font-family : 'Chalet Comprime Cologne Sixty';

      }

    </style>

    <style>

      .layer {

        overflow-x : hidden;

      }

      @media( min-width : 992px ) {

        .layer {

          overflow-x : visible;

        }

      }

      .group {

        transition : transform 300ms ease;

      }

      .stage-open .group {

        transform : translateX( -250px );

      }

      .stage-shelf {

        transition : transform 300ms ease;

        -webkit-overflow-scrolling : touch;

        transform : translateX( 250px );

        position : fixed !important;

        z-index : 1090 !important;

        left : auto !important;

        overflow-y : auto;

        right : 0;

      }

      .stage-open + .stage-shelf {

        transform : translateX( 0px );

      }

      [data-toggle="stage"] polyline,
      [data-toggle="modal"] polyline {

        transition : transform 300ms ease;

        transform-origin : 13.333px;

        transform : rotate( 0deg );

      }

      .stage-open [data-toggle="stage"] polyline:first-of-type {

        transform : rotate( -45deg );

      }

      .stage-open [data-toggle="stage"] polyline:last-of-type {

        transform : rotate( 45deg );
 
      }

      .navbar .dropdown:hover .dropdown-menu {

        display : block;

      }
      .fancybox-image, .fancybox-spaceball {
       	    background: white;
        	padding: 1em;
      } 

    </style>
    

    <meta name="google-site-verification" content="xxl6am69ItGROPQl6gJXqcI5x02-ZZr1WVbr6s8wMxA" />
    <script src="https://code.jquery.com/jquery-3.3.1.min.js?ckcachebust=586898796"></script>

    {% include 'minmaxify-head' %}
    
    <script>
   function getUrlVars() {
      var vars = {};
      var parts = window.location.href.replace(/[?&]+([^=&]+)=([^&]*)/gi, function(m,key,value) {
          vars[key] = value;
      });
      return vars;
  }
  function getUrlParam(parameter, defaultvalue){
      var urlparameter = defaultvalue;
      if(window.location.href.indexOf(parameter) > -1){
          urlparameter = getUrlVars()[parameter];
          }
      return urlparameter;
  }
  var _defaultValue = false;
  var checkUnifyCheckout = getUrlParam('unify', _defaultValue);
  if(checkUnifyCheckout)
  {
    var scriptTag = document.createElement("script");
    scriptTag.src ="https://platform.unify.to/assets/js/shopify.js";
    document.getElementsByTagName('head')[0].appendChild(scriptTag);
   var unifyWhitelistedCheckoutDomain='checkout.drinkkiito.com';
  
  }
</script>
        <!-- tiktok pixel -->
    <script>
      (function () {
        var ta = document.createElement('script'); ta.type = 'text/javascript'; ta.async = true;
        ta.src = 'https://analytics.tiktok.com/i18n/pixel/sdk.js?sdkid=BRCL8D8UB1JFH3P9SD8G';
        var s = document.getElementsByTagName('script')[0];
        s.parentNode.insertBefore(ta, s);
      })();
    </script>
    <!-- END tiktok pixel -->

 
{% render 'pagefly-header' %}

  <script>
    $(function () {
      $("[multiple]").selectable({

      });
    });
  </script>
{%- comment -%}Tapjoy snippet{%- endcomment -%}
{% include 'tapjoy' %}
 </head> 

<body class="bg-dark">

<div class="cart-flyout px-4" style="z-index : 1060">

  <div class="cart-flyout--inner">
    <a href="javascript:;" class="btn-close-cart"><i class="fas fa-times"></i></a>
    <div class="cart-flyout__content">
      <h1 class="cart-flyout__heading text-uppercase">{{ 'layout.cart.mini_cart' | t }}</h1>
      <div class="widget_shopping_cart_content">
          {%- if cart.item_count > 0 -%}
          <ul class="cart_list product_list_widget">
            {%- for item in cart.items -%}
            <li class="mini_cart_item">
              <a href="/cart/change?line={{ forloop.index }}&amp;quantity=0" class="remove" aria-label="Remove this item" onclick="Shopify.removeItem({{ item.id }}); return false">×</a>
              <div class="product-content correct">
                <img width="120" height="150" src="{{ item | img_url: '120x' | split:'?' | first }}" class="attachment-shop_thumbnail size-shop_thumbnail wp-post-image" alt="">
				        <div class="product-info">
                  <span class="product-title">{{ item.product.title }}</span>
					        <span class="quantity">{{ item.quantity }} × <span class="amount">{{ item.price | money }}</span>
                  </span>
                  {% unless item.variant.title contains 'Default' %}
                  <dl class="variation">
                    {% for option in item.product.options %}
                    <dt class="variation-Color">{{ option }}:</dt>
                    <dd class="variation-Color">
                      <p>{{ item.variant.options[forloop.index0] }}</p>
                    </dd>
                    {% endfor %}
                  </dl>
                  {% endunless %}
                </div>
              </div>
            </li>
            {%- endfor -%}
          </ul>
          {%- else -%}
          	<p class="mini-cart__empty-message">{{ 'cart.general.empty' | t }}</p>
          {%- endif -%}
          {%- if cart.item_count > 0 -%}
            <p class="mini-cart__total total">
              <strong>{{ 'cart.general.subtotal' | t }}</strong> <span class="amount">{{ cart.total_price | money }}</span>
            </p>
            <p class="mini-cart__buttons buttons">
              <a href="/checkout" class="button checkout wc-forward">{{ 'cart.general.checkout' | t }}</a>
              <a href="/collections/shop" class="btn btn--secondary cart__continue small--hide cart__submit-control" >
                {{ 'cart.general.continue_shopping' | t }}
              </a>
            </p>
          {%- endif -%}
      </div>
    </div>
  </div>
</div>
<div class="la-overlay-global" class="z-index:9"></div>

    <div class="stage bg-dark" id="stage">

      <div class="fixed-top" style="z-index : 8">

        <div class="layer">

          <div class="group">

            {% section 'navigation' %}

          </div>

        </div>

      </div>

      <div class="layer main-layer">

        <div class="group">

          <div class="main">

            {{ content_for_layout }}

          </div>

          <h1 class="text-uppercase mb-4 pt-5" style="text-align:center">Superfuel your feed</h1>
          <div id="insta-feed"></div>

          {% section 'footer' %}

        </div>

      </div>

    </div>



    <div class="stage-shelf bg-dark p-0">

      <div class="menu h-100">

        <div class="carousel slide h-100" id="menu">

          <div class="carousel-inner h-100">

            <div class="carousel-item active h-100">

              <ul class="nav flex-column py-2">

                {% assign to = 1 %}

                {% if customer.tags contains 'Wholesale' %}
                
                <li class="nav-item">
                
                  <a class="nav-link text-uppercase px-3 py-2" href="https://drinkkiito.com/collections/wholesale">
                
                    Wholesale
                
                  </a>
                
                </li>
                {% endif %}

                {% for link in menus['main-menu'].links %}

                  <li class="nav-item">

                    {% if.link.links.size > 0 %}

                      <a class="nav-link" data-slide-to="{{ to }}" href="#menu">

                        {{ link.title }} &rarr;

                      </a>

                      {% assign to = to | plus : 1 %}

                      {% for link in link.links %}

                        {% if link.links.size > 0 %}

                          {% assign to = to | plus : 1 %}

                        {% endif %}

                      {% endfor %}

                    {% else %}

                      <a class="nav-link text-uppercase px-3 py-2" href="{{ link.url }}">

                        {{ link.title }}

                      </a>

                    {% endif %}

                  </li>

                {% endfor %}

              </ul>

            </div>

            {% assign slide = 1 %}

            {% for link in menus['main-menu'].links %}

              {% if link.links.size > 0 %}

                <div class="carousel-item">

                  <ul class="nav flex-column">

                    <li class="nav-item">

                      <a class="nav-link active py-3" data-slide-to="0" href="#menu">

                        &larr; {{ link.title }}

                      </a>

                    </li>

                    {% assign to = slide | plus : 1 %}

                    {% for link in link.links %}

                      <li class="nav-item">

                        {% if link.links.size > 0 %}

                          <a class="nav-link" data-slide-to="{{ to }}" href="#menu">

                            {{ link.title }} &rarr;

                          </a>

                          {% assign to = to | plus : 1 %}

                        {% else %}

                          <a class="nav-link" href="{{ link.url }}">

                            {{ link.title }}

                          </a>

                        {% endif %}

                      </li>

                    {% endfor %}

                  </ul>

                </div>

                {% assign to = slide %}

                {% assign slide = slide | plus : 1 %}

                {% for link in link.links %}

                  {% if link.links.size > 0 %}

                    <div class="carousel-item">

                      <ul class="nav flex-column">

                        <li class="nav-item">

                          <a class="nav-link active py-3" data-slide-to="{{ to }}" href="#menu">

                            &larr; {{ link.title }}

                          </a>

                        </li>

                        {% for link in link.links %}

                          <li class="nav-item">

                            <a class="nav-link" href="{{ link.url }}">

                              {{ link.title }}

                            </a>

                          </li>

                        {% endfor %}

                      </ul>

                    </div>

                    {% assign slide = slide | plus : 1 %}

                  {% endif %}

                {% endfor %}

              {% endif %}

            {% endfor %}

          </div>

        </div>

      </div>

    </div>

    <script src="https://cdn.jsdelivr.net/gh/fancyapps/fancybox@3.5.6/dist/jquery.fancybox.min.js?ckcachebust=586898796"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.js?ckcachebust=586898796"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js?ckcachebust=586898796"></script>



    {% include 'script-tags' , layout : 'theme' %}


    {% include 'ajaxify-cart' %}




<!-- Drip -->
<script type="text/javascript">
  var _dcq = _dcq || [];
  var _dcs = _dcs || {};
  _dcs.account = '2939181';

  (function() {
    var dc = document.createElement('script');
    dc.type = 'text/javascript'; dc.async = true;
    dc.src = '//tag.getdrip.com/2939181.js';
    var s = document.getElementsByTagName('script')[0];
    s.parentNode.insertBefore(dc, s);
  })();
</script>
<!-- end Drip -->

<script text="text/javascript">
  var _learnq = _learnq || [];

  var item = {
    Name: {{ product.title|json }},
    ProductID: {{ product.id|json }},
    Categories: {{ product.collections|map:'title'|json }},
    ImageURL: "https:{{ product.featured_image.src|img_url:'grande' }}",
    URL: "{{ shop.secure_url }}{{ product.url }}",
    Brand: {{ product.vendor|json }},
    Price: {{ product.price|money|json }},
    CompareAtPrice: {{ product.compare_at_price_max|money|json }}
  };

  _learnq.push(['track', 'Viewed Product', item]);
  _learnq.push(['trackViewedItem', {
    Title: item.Name,
    ItemId: item.ProductID,
    Categories: item.Categories,
    ImageUrl: item.ImageURL,
    Url: item.URL,
    Metadata: {
      Brand: item.Brand,
      Price: item.Price,
      CompareAtPrice: item.CompareAtPrice
    }
  }]);
</script>

{% include 'limitsify' %}
  {% include 'subscription-theme-footer' %}

	        </body>


</html>

